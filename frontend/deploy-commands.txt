## GitHub Repository Setup Commands

After creating the repository on GitHub, run these commands:

cd "C:\Users\BADSHAH JAN\Desktop\CyberVerseWeb\frontend"
git push -u origin main

## Netlify Deployment Steps

1. Go to https://app.netlify.com/
2. Click "Add new site" > "Import an existing project"
3. Choose "Deploy with GitHub"
4. Select your "CyberVerseWeb" repository
5. Configure build settings:
   - Build command: npm run build
   - Publish directory: out
   - Node version: 18
6. Click "Deploy site"

## Environment Variables (if needed)
No environment variables required for this static deployment.

## Custom Domain (optional)
After deployment, you can add a custom domain in Site settings > Domain management.